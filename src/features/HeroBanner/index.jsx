import { ContentWrapper } from '@/components/ContentWrapper';
import styles from './style.module.scss';

export const HeroBanner = () => {
  return (
    <section className={styles.hero}>
      <ContentWrapper>HeroBanner</ContentWrapper>
    </section>
  );
};
